name: jocaagura_navigator
description: "A navigator route manager"
version: 1.0.0
homepage: https://github.com/jocaagura/jocaagura_navigator

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:

